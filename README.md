```bash
# Install dependencies
pnpm install

# Create a .env file based on the example
DATABASE_URL=postgres://username:password@host:PORT/your_database_name
NODE_ENV=development
PORT=9999

# Run the development server
pnpm dev

# Open the app in your browser
open http://localhost:9999
# Explore the open api on
open http://localhost:9999/refernce
```
